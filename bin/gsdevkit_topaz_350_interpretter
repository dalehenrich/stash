#!/usr/bin/env bash
#
#	command line interpretter for topaz script
#	
#	using 3.5.0 and beyond topaz commands
#

topazFile=`realpath $1`
shift

topazArgs="$*"
if [ $# -eq 0 ] ; then
	topazArgs="$GEMSTONE_SCRIPT_ARGS"
fi

$GS_HOME/bin/scriptTopaz $topazArgs -l -S $topazFile
